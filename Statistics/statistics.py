from calculator.calculator import calculator


class Statistics(calculator):

    class Statistics(Calculator):

        def mean(self, data):
            self.result = mean(data)
            return self.result